{{- $serviceAccessManager := .Values.serviceAccessManager }}
{{- if and $serviceAccessManager.deploy $serviceAccessManager.namespace.create }}
---
apiVersion: v1
kind: Namespace
metadata:
  labels:
    {{- include "babylon.labels" $ | nindent 4 }}
    app.kubernetes.io/component: service-access-manager
  name: {{ $serviceAccessManager.namespace.name }}
{{- end }}
