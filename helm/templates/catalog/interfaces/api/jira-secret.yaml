{{- range $namespace, $interface := .Values.catalog.interfaces }}
---
apiVersion: bitwarden-k8s-secrets-manager.demo.redhat.com/v1
kind: BitwardenSyncSecret
metadata:
  name: babylon-catalog-jira
  namespace: {{ $namespace }}
spec:
  data:
    jira-api-token:
      secret: jira_credentials
      key: api_token
    jira-user-email:
      secret: jira_credentials
      key: user_email
    jira-base-url:
      secret: jira_credentials
      key: base_url
    jira-project-key:
      secret: jira_credentials
      key: project_key
{{- end }}
